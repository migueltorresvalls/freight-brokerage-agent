declare module 'react-simple-maps' {
  // Minimal typings for the components we use. For this dashboard we don't
  // rely on the library's TypeScript types, so `any` is sufficient.
  export const ComposableMap: any
  export const Geographies: any
  export const Geography: any
  export const Marker: any
}

